// import { z } from "zod";

// war ursprünglich: import { createTRPCRouter, publicProcedure } from "@/server/api/trpc";


import { createTRPCRouter } from "@/server/api/trpc";

export const postRouter = createTRPCRouter({});
